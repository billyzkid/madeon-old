export const AppState = {
  default: "default",
  loading: "loading",
  loaded: "loaded",
  failed: "failed"
};

export const AudioType = {
  drum: "drum",
  bass: "bass",
  sound: "sound"
};

export const ErrorType = {
  none: "none",
  loadAudioContext: "loadAudioContext",
  loadImage: "loadImage",
  other: "other"
};